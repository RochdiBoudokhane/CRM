@model IEnumerable<Solution.Presentation.Models.ClaimCRM>

@{
    ViewBag.Title = "Stat";
    Layout = "~/Views/Shared/_MyLayout.cshtml";
}

<div class="all-title-box">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">

                <ul class="breadcrumb">
                    <li class="breadcrumb-item">
                       



                        <a href="~/Claim/Create"> </a>
                    </li>
                    @*<li class="breadcrumb-item active">Wishlist</li>*@
                </ul>
                <table class="table">
                    <tr>
                        @*<th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.NumTel)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ImageUrl)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Message)
                </th>*@
                        @*<th>
                    @Html.DisplayNameFor(model => model.CreatedDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.State)
                </th>*@

                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            @*<td>
                        @Html.DisplayFor(modelItem => item.Name)
                     </td>
                         <td>
                        @Html.DisplayFor(modelItem => item.NumTel)
                         </td>
                         <td>
                        @Html.DisplayFor(modelItem => item.Email)
                     </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ImageUrl)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Type)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Message)
                    </td>*@
                            <td>
                                @Html.DisplayFor(modelItem => item.somme)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.somme)
                            </td>
                            <td class="col-lg-12">

                                @{
                                    if (item.State == 0)
                                    {
                                        @Html.ActionLink("Non traité ", "Delete", new { id = item.ClaimId })
                                    }
                                    if (item.State == 1)
                                    {
                                        @Html.ActionLink("En cours de traitement ", "Delete", new { id = item.ClaimId })
                                    }
                                    if (item.State == 2)
                                    {
                                        @Html.ActionLink("Traité", "Delete", new { id = item.ClaimId })
                                    }
                                }


                                @*@Html.ActionLink("Traité", "Edit", new { id = item.ClaimId }) |*@

                                @*@Html.ActionLink("Suivi", "Suivi", new { id = item.ClaimId }) |*@







                            </td>
                        </tr>
                    }

                </table>
            </div>
        </div>
    </div>
</div>
<